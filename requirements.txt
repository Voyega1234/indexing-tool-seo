# Indexing Tool — Python dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.6
# Postgres (Supabase) storage
psycopg[binary,pool]>=3.1
# Only needed for the optional Indexing API nudge (service-account JWT signing).
# The rest of the tool (crawl, checkup, diagnosis, sitemap submit) runs without it.
# `requests` is google-auth's token-refresh transport.
google-auth>=2.28
requests>=2.31
